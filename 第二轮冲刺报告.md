# 《第二轮冲刺报告》喵星图工厂 CatGalaxy Factory

- 时间窗：2026-09-06 23:25（第一轮交付提交）→ 2026-09-07 04:20，无人值守，用户授权「全部权限，自动做到 07:50」
- 起点：第一轮 [交付报告.md](交付报告.md) 的「次日待办」11 条 + 「已知限制」里被点名的第 3/5/8 条
- 结果：**11 条待办全部落地**，19 个功能点提交，用例 165 → **648**，acceptance **119 PASS / 0 FAIL**
- 现状文档：[README.md](README.md)（数字已刷新）；第一轮报告**原文未改**，只在被推翻的段落就地加了 `（第二轮：…）` 标注

---

## 一、次日待办逐条兑现

第一轮待办的编号沿用原文，一条不落。

| # | 第一轮待办 | 第二轮做了什么 | 提交 | 用例 |
|---|---|---|---|---|
| 1 | F8 底图标定 | 预览页在真实底图上拖星定位，坐标以归一化 0~1 存 `calib.json`，生成时烘焙进 `CALIB`；越界服务端拒绝；`DELETE` 整体退回算法推导。`GET/PUT/DELETE /api/projects/{pid}/calib` | `c86e325` `221154b` | `test_calib.py` 26 |
| 2 | 归并工作台（F9） | 候选组并排对比（照片 + 特征 + 场景），一键「同猫 / 不同猫」，**判定必须写理由**，写回 `merge.json` 与操作日志，`DELETE /merge/{gid}` 可撤销 | `a6c3b5a` `c8fdc45` | `test_merge.py` 45 |
| 3 | 名册在线编辑（F10） | `GET /roster` 读原始行与列映射、`PATCH /roster` 按「行号 + 列名」改单元格，原子回写 `roster.csv`，每处改动带旧值/新值进操作日志，改完自动重跑校验 | `a6c3b5a` `c8fdc45` | `test_roster_edit.py` 20 |
| 4 | zip 递归 + 体积/数量上限 | 递归解包到 3 层；四道上传限额（单文件 200MB / 单次 2000 张 / 项目累计 3000 张 / zip 条目 20000）+ 两道防炸弹护栏（累计解压 1GB / 流式块 1MB）；任一超限**整请求回滚** | `2544f1a` | `test_zip_limits.py` 29 |
| 5 | 大项目耗时优化 | 压缩多进程并行 + 两种形态流式落盘，详见第二节 | `6ed6af1` `1e5879c` `42aef87` | `test_parallel_compress.py` 22、`test_bundle_streaming.py` 123 |
| 6 | 项目列表分页 + 清理命令 | 列表分页搜索；`scripts/cleanup.py`（`--before 7d` / `--keep-latest` / `--stats` / `--json`），**默认干跑，`--apply` 才动手** | `434d2eb` | `test_project_list_and_cleanup.py` 56 |
| 7 | 并发保护 | 项目级写互斥（Semaphore，超时 180s）+ 状态文件原子替换 + 读侧撞 WinError 32/33 重试 | `320b502` `e061757` | `test_concurrency.py` 44 |
| 8 | 星图主题化 + LOGO（F11） | 5 套配色预设、5 套字体栈、可调颜色白名单、页脚署名、校徽（重编码为 ≤256px PNG，relative 走文件、inline 走 base64） | `2349eb8` `4c8264e` | `test_theme.py` 63、`test_console.py` 8 |
| 9 | 前端进度条 | `progress.json` + `GET /progress` 轮询，上传压缩与生成打包全过程可见（不再只有 Toast） | `40fb1b7` | `test_progress.py` 38 |
| 10 | 《跨校落地手册》 | `docs/跨校落地手册.md`：普查到成品完整 SOP + 脏 CSV 逐码处置清单 | `416630f` | — |
| 11 | CI | `.github/workflows/ci.yml`：ubuntu + windows 双跑 `pytest -q` 与 `acceptance.py --case demo2`，证据作 artifact 上传；给 F6 补了无 E 盘兜底 | `1d9db17` | `test_ci.py` 8 |

第一轮「已知限制」里被点名的三条也一并消掉：第 3 条（CALIB 只是推导坐标）→ F8；第 5 条（zip 只解一层）→ #4；第 8 条（无并发写保护）→ #7，其中「无鉴权 / 单机 / 不上公网」是有意保留的。

新增 API 端点 16 个：F8 3 / F9 3 / F10 2 / 进度 1 / F11 7。

---

## 二、三个性能改动：实测数字与方法

数据源都是中北全量真实数据（76 只猫 / 75 张手机原图 266.7MB），测量工具是 `tracemalloc` 峰值与 `time.perf_counter`，脚本跑完即删，不留进仓库。

| 改动 | 改前 | 改后 | 代价 |
|---|---|---|---|
| 照片压缩多进程并行（4 工 / 每批 16 张 / <4 张串行） | 串行 **16.35s** | 并行 **6.35s**（2.58x） | 建池开销；4 张以下不划算所以仍走串行 |
| inline 分片流式写出（只 `stat` 定边界，逐片读盘编码） | 生成峰值 **87.21MB** | **11.4MB**（分片路径本身 10.92MB） | 时间不变（0.87s → 0.88s） |
| relative 产物流式落盘（照片逐张读、逐张写） | 生成峰值 **14.81MB** | **0.98MB** | 时间不变（0.63s） |

三件事都值得说清楚的部分：

1. **流式换的是内存，不是时间。** 两种形态的耗时都在噪声内（0.88s / 0.63s），我没有把它写成「性能提升 X 倍」。真正的收益是峰值**不再随照片数增长**：inline 在 10 / 30 / 75 张照片下峰值分别是 7.43 / 10.22 / 10.92MB——决定峰值的是 `PHOTO_CHUNK_BYTES = 1.5MB`，不是图廊大小。按改前的写法，照片数上限 3000 张、单张上限 200KB，顶格要约 **3.5GB**。
2. **产物逐字节相同才算重构成功。** inline 改完后 15 个产物文件（18.52MB）与改前**完全一致**，比对方式是把改前的算法原样抄进测试文件当 oracle（`tests/test_bundle_streaming.py::_legacy_chunks`），连输入顺序打乱都不影响输出。relative 同理（14.00MB / 76 条 CATS / 75 张照片不变）。
3. **新引入的不变量必须自己盯住。** 规划分片时只 `stat` 不 `read`，靠 `base64 长度恒为 4*ceil(n/3)` 预测行长，而行的外壳长度用 `_photo_line(name, b"")` 现量——这样将来改转义规则也不会让两边失同步。一旦失同步，HTML 里的 `<script src>` 会指向不存在的文件，**星图打开就是一片黑且不报错**。三层测试盯这条：单元层（规划名 == 实际写出名，7 种张数 × 4 种分片大小）、字节层（对 oracle 逐字节相等）、路由层（20 张真实照片自然产生 ≥2 片，断言「每个 `<script src>` 都有对应文件，且每个文件都被引用」）。

`build_photo_chunks` 被删掉而不是留着当兼容壳：应用内已无人调用，留着就是死代码；等价性证明搬进测试即可。

---

## 三、验收与测试证据

### 3.1 单测

```
.venv\Scripts\python -m pytest -q     # 退出码 0
```

- **648 条用例全绿**：进度输出 9 行 × 72 个点 = 648，无一个 `F` / `E` / `s`；`pytest --collect-only -q` 独立数出 20 个文件 / 648 条，两边对齐。
- 原始输出另存 [`docs/测试输出-第二轮.txt`](docs/测试输出-第二轮.txt)（第一轮的 `docs/测试输出.txt` 未改动）。
- **诚实说明**：本机这份 pytest **不打印 `N passed in Xs` 汇总行**（第一轮报告里那行 `165 passed in 27.16s` 是当时环境打出来的）。所以本轮改用「退出码 + 点数 + `--collect-only` 计数」三方互证，而不是引用一行不存在的汇总。

20 个测试文件的分布：

| 文件 | 例数 | 文件 | 例数 |
|---|---|---|---|
| `test_bundle_streaming.py` | 123 | `test_image_proc.py` | 27 |
| `test_theme.py` | 63 | `test_calib.py` | 26 |
| `test_project_list_and_cleanup.py` | 56 | `test_injector.py` | 16 |
| `test_merge.py` | 45 | `test_packager.py` | 14 |
| `test_concurrency.py` | 44 | `test_csv_loader.py` | 13 |
| `test_progress.py` | 38 | `test_ci.py` | 8 |
| `test_star_mapper.py` | 30 | `test_console.py` | 8 |
| `test_api_e2e.py` | 29 | `test_validate.py` | 17 |
| `test_zip_limits.py` | 29 | `test_parallel_compress.py` | 22 |
| `test_census_parser.py` | 20 | `test_roster_edit.py` | 20 |

### 3.2 验收

```
.venv\Scripts\python scripts\acceptance.py --case all     # 退出码 0
```

**119 PASS / 0 FAIL**（场景 1 空骨架 2 行：59；场景 2 真实名册 76 只：60），证据已刷新到 `docs/验收证据-场景1-空骨架2行.md` 与 `docs/验收证据-场景2-全量76只.md`。

场景 2 的关键值与第一轮一致，说明重构没动数据面：照片定位 75/75、入库 75 张跳过无、CATS 76 条、inline 分片 15 个、未内嵌 0、relative/inline 预览均 HTTP 200。

一处**变长**了：F7 归并决策摘要 2531 → **3700 字符**。原因是 F9 落地后摘要多了「## 六、归并判定记录（人工留痕）」——候选组、判定、保留/弃用编号与人工写的理由都进了表格，摘要第一次有了真实决策依据可写，不再只是骨架。

---

## 四、第二轮自主决策记录

| # | 决策点 | 选择 | 理由 |
|---|---|---|---|
| 1 | 项目锁的实现 | Semaphore（超时 180s），**不是** RLock | 真踩到的坑：`RLock` 只能在持有线程里 `release`，异常路径换个线程释放会抛 `RuntimeError`，项目就此**永久锁死**。`e061757` 是修 bug，不是预防性设计 |
| 2 | 并行压缩的粒度 | 4 工作者 / 每批 16 张 / 不满 4 张串行 | 批内并行、批间串行是为了不把几百 MB 原图同时压进内存；不满 4 张时建池成本高于收益，而且 Windows 是 `spawn`，子进程要重导入主模块 |
| 3 | 分片边界怎么定 | 只 `stat` 不 `read`，用 `4*ceil(n/3)` 预测 base64 长度 | 边界必须与真实编码时一致，否则星图打开一片黑；预测法让规划阶段零读盘 |
| 4 | 行长外壳怎么量 | 调 `_photo_line(name, b"")` 现量，不手写常量 | 转义规则将来一改，手写常量就会失同步；现量则永远同步 |
| 5 | 旧函数留不留 | 删掉 `build_photo_chunks` / `_photo_blobs` | 应用内无人调用即死代码；等价性用测试里的 `_legacy_chunks` oracle 逐字节对照，比留个壳更硬 |
| 6 | 重构时保留的「怪」语义 | 大小写不匹配的照片名**仍**进 `missing_photos`；`map.jpg` **仍**被底图覆盖 | 这两条是既有行为，重构不该顺手改语义。后者补了专门用例（同名照片不得顶掉底图）把它锁死 |
| 7 | 标定坐标的存法 | 归一化 0~1，不存像素 | 换底图、换分辨率、换设备都不用重标 |
| 8 | 归并判定要不要强制理由 | 强制。判「同猫」或「不同猫」都必须写理由 | F7 摘要要的是可追溯依据，只有结论没有理由等于没有留痕 |
| 9 | 名册在线编辑写不写原文件 | 直接原子改写 `roster.csv`，每处改动带旧值/新值进操作日志 | 不引入第二份真相。代价是「未动的原件」不再存在，README 第九节已明确写出，要留原件请自己另存 |
| 10 | 主题的安全边界 | 字体只接受白名单 key（value 由服务端持有，用户碰不到）；颜色走白名单校验；署名按**纯文本转义**追加在出处之后，不替换出处 | 主题值会直接进 `<style>` 与 HTML，是注入面。出处不可替换是数据红线（不篡改来源标注） |
| 11 | CI 跑哪个验收场景 | 只跑 `demo2`，且给它补无 E 盘兜底 | `full76` 依赖 E 盘总库的真实名册与 75 张原图，那是本地实测证据，进不了 CI。矩阵里放 Windows 不是凑数：原子写重试认的是 WinError 32/33，GBK 控制台的坑也只在 Windows 出现 |
| 12 | 清理脚本的默认动作 | 默认干跑，`--apply` 才真删 | 删 `workspace/` 不可逆 |
| 13 | 第一轮报告怎么处理 | 就地加 `（第二轮：…）` 标注，原文一字不改 | 交付记录是过程证据，改写它就等于销毁证据。被推翻的结论要看得见「曾经是什么、为什么变」 |

---

## 五、仍存在的限制（第二轮之后）

1. **仍无可视化截图**：内置浏览器 viewport `0x0`，`take_screenshot` 返回 `NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`。取证方式与第一轮相同（可访问性树快照 + `evaluate_script` 读 canvas 像素 + 真实 HTTP 断言），证据强度等价，但没有图。人工打开 `http://127.0.0.1:8010/` 即可看到完整界面。
2. **F8 标定是逐星拖点**：76 只手标一遍要十几分钟，没有「按分区整体平移/吸附」这类批量手段。
3. **F9 候选组仍是启发式**（主色 + 出没区），不做图像相似度；同色同区的两只不同猫还是会进同一候选组，仍需人工判。
4. **名册在线编辑只能改单元格**，不支持增行/删行；要加猫仍得改 CSV 重传。
5. **流式改动省内存不省时间**（见第二节），别把它当耗时优化。
6. **并行工数写死 4**，不按机器负载自适应；CPU 核多的大机器上还有余量没吃。
7. **多猫同框照片**仍需人工归并后拆行，解析器只告警。
8. **无鉴权、单机单用户**：只监听 127.0.0.1，有意保留。
9. **仓库没有 git 远端**：CI 工作流文件已就位并有 8 条用例盯着它的内容，但**从未真正跑过一次**——推到 GitHub 才算验证。

---

## 六、红线自查（第二轮）

| 红线 | 状态 |
|---|---|
| 严禁修改 E 盘任何文件 | ✅ 本轮全程只读。`E:\猫咪星图_总库\14_喵星图工厂产出\` 是第二轮初新建的**纯新增**归档（`cp -n`，脚本前置 `if [ -e "$DST" ]` 检查，目标存在即中止），E 盘既有 00~13 目录与原文件一个字节未动。溯源细节见第一轮报告 §六 补注（`4476d4f`） |
| 严禁改动 `D:\code`、`D:\校园基米skill` | ✅ 未触碰，所有写入限定在 `D:\QoderProjects\catgalaxy-factory\` |
| 不部署公网、不注册外部服务 | ✅ 服务只监听 `127.0.0.1`；验证用的临时 dev server（端口 8012，PID 31840）在收尾时已确认命令行并停止，端口已释放 |
| 不下载 >100MB 依赖 | ✅ **本轮没有新增任何第三方依赖**，并行用的是标准库 `concurrent.futures` |
| 所有产出仅写入项目根目录 | ✅ 代码、测试、文档、验收产物、`workspace/` 均在项目内；性能测量脚本跑完即删（`git status` 干净） |
| 需决策处自选默认并记录 | ✅ 见第四节 13 条 |

---

## 七、如果还有第三轮

按「对交付质量的影响」排序：

1. **真浏览器可视化回归**：viewport 可用后补 Playwright 截图断言，把第一/二轮都缺的那块证据补齐。
2. **F9 加感知哈希预排序**：候选组内先按图像相似度排序，人工判定量能从「每组都看」降到「只看前几名」。
3. **F8 批量标定**：按分区整体平移/缩放 + 吸附到底图上的建筑物轮廓，把十几分钟压到一两分钟。
4. **名册在线编辑支持增删行**：现在只能改单元格，加猫仍要往返 CSV。
5. **推远端让 CI 真跑一次**：工作流没跑过就等于没验证。
6. **清 `workspace/`**：约 30 个验收/基准跑测留下的项目目录（`1788707275-中北大学` 是真实验收数据，要留）。命令已就位：`scripts/cleanup.py --before 1d --apply`，默认干跑可先看清单。
7. **并行工数自适应**：按 `os.cpu_count()` 与当前负载定工数，别写死 4。

---

## 附：第二轮提交清单（19 个，边写边提交，每次提交前 `pytest -q` 全绿）

```
42aef87 04:03 perf: relative 产物也流式落盘——生成峰值 14.81MB → 0.98MB
1e5879c 03:58 perf: inline 分片流式写出——生成峰值 87.21MB → 10.92MB，且不再随照片数增长
6ed6af1 03:46 perf: 照片压缩多进程并行——75 张原图 16.35s → 6.35s（2.58x）
1d9db17 03:23 ci: GitHub Actions 双平台跑 pytest + acceptance demo2，并给 F6 补无 E 盘兜底
416630f 03:17 docs: 《跨校落地手册》——普查到成品的完整 SOP + 脏 CSV 逐码处置清单
4c8264e 03:13 test(F11): 主题白名单/烘焙/转义 63 例 + 控制台静态接线 8 例 + 验收闭环
2349eb8 03:13 feat(F11): 星图主题化——配色预设 / 字体栈 / 页脚署名 / 校徽
40fb1b7 02:25 feat: 长任务进度条——上传压缩与生成打包全过程可见
e061757 02:24 fix: 项目锁换成 Semaphore——RLock 跨线程释放失败会把项目永久锁死
320b502 01:53 fix: 项目级并发保护——写路由互斥锁 + 状态文件原子替换 + 读侧共享冲突重试
434d2eb 01:35 feat: 项目列表分页搜索 + workspace 清理脚本
2544f1a 01:24 feat: zip 递归解包 + 四道上传限额，超限整体回滚
c8fdc45 01:14 feat(F9+F10): 归并工作台与名册在线编辑前端
a6c3b5a 01:06 feat(F9+F10): 归并工作台与名册在线编辑后端
4476d4f 00:58 docs: 红线自查补注第二轮——E 盘用户另存文件溯源 + 14_ 归档为纯新增
221154b 00:58 test(F8): 验收脚本补标定闭环——存/读/越界拒绝/烘焙/清除
c86e325 00:52 feat(F8): 底图标定——人工星位覆盖算法推导坐标
```

（另有本次收尾提交：README 数字刷新 + 第一轮报告标注 + 本报告 + 第二轮单测输出证据。）

代码规模：`app/` 18 文件 5050 行 / `static/` 3 文件 1545 行 / `tests/` 20 文件 5955 行 / `scripts/` 2 文件 811 行 / `docs/*.md` 4 份 625 行（另有两份 pytest 原始输出 `.txt` 证据，不计入）。
