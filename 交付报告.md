# 《交付报告》喵星图工厂 CatGalaxy Factory

- 交付时间：2026-09-07 00:00 前（冲刺窗口 22:00–08:00，实际主体完成于 23:20）
- 项目根目录：`D:\QoderProjects\catgalaxy-factory\`
- 技术栈：Python 3.12 + FastAPI + Pillow + 原生 JS 单页；**无数据库**（文件系统 `workspace/{pid}/` 即状态）
- 依赖体积：venv 内 fastapi / uvicorn / python-multipart / pillow / pytest / httpx，均远小于 100MB 红线
- 规格文档：[`SPEC.md`](SPEC.md)　使用说明：[`README.md`](README.md)

---

## 一、完成清单（F1–F7 全部完成）

| 编号 | 功能 | 状态 | 主要实现 | 测试 |
|---|---|---|---|---|
| F1 | 数据导入（12 列 CSV + 照片多选/zip） | ✅ | `app/csv_loader.py`（编码探测 + 列名别名）、`app/image_proc.py`（长边≤1200px、单张≤200KB） | `tests/test_csv_loader.py`、`tests/test_image_proc.py` |
| F2 | 校验与报告 | ✅ | `app/validate.py`（列完整性 / 编号格式·重复·连续性 / 必填 / 照片匹配 / 置信度分级 / 特征过短 / 未使用照片） | `tests/test_validate.py` |
| F3 | 星图生成（两种打包形态） | ✅ | `app/star_mapper.py`（毛色→星色、照片数→星等、出没区→分区、确定性星位）、`app/injector.py` + `app/templates/starmap.html`（token 注入） | `tests/test_star_mapper.py`、`tests/test_injector.py` |
| F4 | 在线预览 | ✅ | `/api/projects/{pid}/preview` + `/bundle` 静态挂载，控制台 iframe 即时预览 | `tests/test_api_e2e.py`、浏览器实测 |
| F5 | 一键打包 zip | ✅ | `app/packager.py`，命名 `{校名}-校园猫咪星图-{日期}.zip` | `tests/test_packager.py` |
| F6 | 普查 batch 解析器（加分项） | ✅ | `app/census_parser.py` + `/api/census/parse`，输出 12 列草稿 CSV + 归并建议 + 行级警告 | `tests/test_census_parser.py` |
| F7 | 归并决策摘要生成器（加分项） | ✅ | `app/summary_writer.py` + `/api/projects/{pid}/summary`，摘要随 zip 交付 | `tests/test_api_e2e.py`、浏览器实测 |

代码规模：后端 2031 行 / 前端 640 行 / 测试 1641 行（8 个测试文件）。

Git：`git init` 已完成，**9 个按功能点分组的提交**：

```
20e55f7 feat(F7): 归并决策摘要生成器
e000ffa feat(F6): 普查 batch 解析器
7eeba96 feat(ui): 五步控制台前端
91ad0aa feat(F4): API 层与在线预览
6ea9cf9 feat(F5): 两种形态打包
fb617fb feat(F3): 星位映射与模板注入
6973bc7 feat(F2): 校验引擎与报告
11d4a72 feat(F1): 名册 CSV 导入与照片压缩入库
84fa5ac chore: 项目骨架与 SPEC.md
```

> **诚实说明**：这 9 个提交是开发完成后按功能点**回溯分组**提交的，不是边写边提交。
> 原因是本次为无人值守冲刺，先保证功能与测试闭环，再整理提交历史。每个提交的文件归属
> 与功能点是真实对应的（F2 提交只含 `validate.py` 与其测试，以此类推）。

---

## 二、验收证据

四份证据文档均在 `docs/`，全部由脚本/浏览器真实执行产出，不是手写描述：

| 文件 | 对应验收标准 | 结论 |
|---|---|---|
| `docs/验收证据-场景1-空骨架2行.md` | 标准 1（`12_跨校复制包` 空模板 + 2 行示例） | 全部 PASS |
| `docs/验收证据-场景2-全量76只.md` | 标准 2（真实名册 76 只全量回归） | 全部 PASS |
| `docs/验收证据-浏览器实测.md` | 标准 1/2 的 UI 侧证据（真实浏览器驱动） | 全部通过，截图受限（见「已知限制」） |
| `docs/测试输出.txt` | 标准 3（单测全绿） | `pytest -q` 输出原文 |

复现命令：

```bash
cd D:\QoderProjects\catgalaxy-factory
.venv\Scripts\python -m pytest -q            # 单测
.venv\Scripts\python scripts\acceptance.py --case all   # 两个验收场景，退出码 0 = 全 PASS
```

### 2.1 单测（标准 3）

`pytest -q` 输出见 `docs/测试输出.txt`：

```
165 passed in 27.16s
```

165 个用例全绿，0 失败 / 0 错误 / 0 跳过（`pyproject.toml` 里 `addopts = "-q"`，故默认输出即为安静模式）。

覆盖模块：CSV 解析（编码/别名/脏数据）、照片压缩（尺寸与体积双红线、EXIF 旋转、PNG/WEBP）、
校验引擎（每条规则一个用例 + 报告 Markdown）、星位映射（确定性、亮度律、分区归类）、
模板注入（token 全部替换、`</` 转义防脚本截断、CALIB 与 CATS 一致）、打包（两形态文件树、
zip 内 LF、命名规范）、API 端到端（含 76 只真实数据回归）。

### 2.2 场景 1 · 空骨架 + 2 行示例（标准 1）

数据源：`E:\猫咪星图_总库\12_跨校复制包\示例校_空数据包骨架\`（只读），表头取自 `猫咪名册_模板.csv`（12 列），
照片用 1600×1200 生成图 2 张。

```
F1 建项目      示例校 → 1788707460-示例校
F1 上传名册    编码 utf-8-sig，解析 2 行，缺列 无，未识别列 无
F1 上传照片    入库 2 张，跳过 无
照片压缩达标   最大长边 1200px，最大单张 197KB；1989KB → 394KB
F2 校验通过    2 行 / 可入图 2 行 / 错误 0 警告 0 提示 0
F2 报告可下载  211 字符 Markdown
F3+F5 relative CATS 2 条、照片 2 张、缺失 0；zip 387KB
F3+F5 inline   CATS 2 条、base64 分片 1 个、未内嵌 0；zip 377KB
F4 预览        /bundle/.../out/relative/示例校喵星图.html → HTTP 200
F6 普查解析    骨架模板 `普查-batch01_模板.txt` 解析 0 条（空模板，符合预期），不报错
F6 真实 batch1 解析 26 条，草稿 CSV 3130 字符 / 26 行，归并建议 2 组，
               首条 0FEE7CB5FD21467F63DAA083BA1FDDFA.jpg → 橘白 / 宿舍区
F7 摘要        1230 字符
```

### 2.3 场景 2 · 真实名册 76 只全量回归（标准 2）

数据源：`E:\猫咪星图_总库\07_普查原始数据\猫咪名册.csv`（76 行）+ `E:\猫咪星图_总库\08_原始照片视频\{照片视频,补充视频照片}`
中定位到的 75 个真实照片文件（原图合计 266.7MB）。

```
照片定位       命中 75 / 75（0 缺失）
F1 上传名册    编码 utf-8-sig，76 行，缺列 无，未识别列 无
F1 上传照片    入库 75 张，跳过 无
照片压缩达标   最大长边 1200px，最大单张 199KB；273122KB → 14157KB（体积降到 5.2%）
F2 校验        76 行 / 可入图 76 行 / 错误 0 / 警告 8 / 提示 28
问题分布       I_ID_GAP×4, I_PHOTO_PATH_NORMALIZED×24, W_BAD_CONFIDENCE×6,
               W_LOW_CONFIDENCE×1, W_PHOTO_SHARED×1
编号范围       CAT-001 ~ CAT-080，空缺 CAT-071/072/076/077（弃用编号，按规格不拦截），
               低置信度 CAT-019（进警告清单）
F3 relative    CATS 76 条、照片 75 张、缺失 0；zip 14227KB
F3 inline      CATS 76 条、base64 分片 15 个、未内嵌 0；zip 14336KB
F4 预览        relative/inline 两个 URL 均 HTTP 200
F5 命名        中北大学-校园猫咪星图-2026-09-06.zip
zip 内容       星图 HTML + assets + data/猫咪名册.csv + 校验报告.md + 归并决策摘要.md
F7 摘要        2531 字符
```

「76 只猫 / 75 张照片」是真实数据本身的属性（有两只猫共用同一张代表照片，触发 `W_PHOTO_SHARED` 警告），
不是缺图：注入后 76 条 CATS 的 `photo` 全部能在包内解析，缺失 0。

### 2.4 浏览器实测（F1–F5 + F7 在真实 Chrome 里走通）

详见 `docs/验收证据-浏览器实测.md`。关键实测值：

- 控制台上传后回显 `2 行 · utf-8-sig` / `2 张 · 194KB`；校验卡片渲染「✅ 校验通过 · 可生成星图」
- 预览 iframe 内读到运行时数据：`CATS.length=2`、`CALIB=2`、`ZONES=2`，
  首条含 `starColor:"255,190,110"`（橘）、`starRadius:3.9`、`brightness:0.5`、`zone:"宿舍居民区"` 与拼装好的 `bio`
- **照片真的加载出来了**：`assets/photos/demo-cat-001.jpg` → `naturalSize 1200x900`；底图 → `1920x1239`
- **canvas 真的画出星点**：1280×800 画布上非暗像素 2915 个，最大通道和 649（星核近纯白）
- 内嵌版：`<script src>` 仅 `assets/photo-data-01.js`；`PH("assets/photos/demo-cat-001.jpg")`
  返回 `data:image/jpeg;base64,/9j/4AA…`，用该 data URL 建 Image 同样 `onload` 成功；
  渲染像素数与相对路径版完全一致（2915）
- 下载：`GET /download?form=relative` → 200 / `application/zip` / 355746 字节 / 前 2 字节 `PK` /
  `content-disposition` 文件名解码为 `示例校-校园猫咪星图-2026-09-06.zip`
- F7：点击后 Toast「归并决策摘要已生成并下载」

---

## 三、自主决策记录（无人值守期间自行选定，均可改）

| # | 决策点 | 选择 | 理由 |
|---|---|---|---|
| 1 | UI 风格 | 深空暗色单页 + 五步竖向流程条（建项目→传数据→看校验→生成预览→打包下载），左侧操作右侧报告/预览 | 与星图产物气质一致；五步条本身就是操作说明书，降低跨校同学的上手成本 |
| 2 | 前端技术 | 原生 JS（无构建、无 CDN 依赖） | 离线可跑、零构建链，符合「不联网下大依赖」红线 |
| 3 | 校园底图 | 未上传时用 Pillow 程序化生成 1920×1239 星野底图 | 空骨架包里没有底图；不能因为缺底图就阻断出图 |
| 4 | 内嵌形态实现 | base64 分片成 `assets/photo-data-NN.js`（单片 ≤1.5MB）+ 运行时 `PH(p)` 解析 | 对齐参考实现 `中北喵星图.html` 的纯文本包做法，避免单个巨型 HTML 卡死浏览器 |
| 5 | 星等律 | `brightness = min(0.5 + 0.11*(照片数-1), 1.05)` | 逐条比对参考实现 76 只猫的数据反推得到，保证跨校产物与原版观感一致 |
| 6 | 星位算法 | FNV-1a 哈希 + mulberry32 → 黄金角螺旋布局 → 最小间距松弛 | 确定性：同一份数据两次生成字节级一致，便于回归对比 |
| 7 | 产物换行 | 所有文本产物强制 `newline="\n"` 写出 | Windows 下 `write_text` 默认 CRLF，会让包内 HTML 与测试断言、跨平台校验不一致 |
| 8 | 照片引用匹配 | 名册里的写法按 **basename** 归一到实际入库文件名，并留 **stem 兜底**（压缩后统一 `.jpg`） | 真实名册混用裸文件名与 `补充视频照片/xxx.jpg` 这类带目录前缀的写法，不归一就会误报 24 条「照片缺失」 |
| 9 | 归一化的位置 | 放在校验层（`validate.py`），就地写回 `row.photo_file` | 单点归一后，报告、注入、打包共用同一份事实，避免每个下游各自打补丁 |
| 10 | 弃用编号 | 只报 `I_ID_GAP`（info），不拦截生成 | 规格明确「弃用编号允许空缺」；数据红线是不复用弃用编号 |
| 11 | 置信度=低 | 出警告清单 + 前端「排除置信度低的行」开关（默认不勾） | 规格要求给警告；是否弃用交给操作者决定，工具不越权 |
| 12 | 场景→出没区 | 关键词两级匹配：先具体地点（宿舍/教学楼/石砖/瓷砖…），再兜底「室内/户外」，都没命中则截前 12 字 | 「室内宿舍：红白格桌布木桌下」这类文本里，「室内」只是修饰词，主场景是宿舍 |
| 13 | 无数据库 | `workspace/{pid}/` 目录 + `meta.json` + 操作日志 | 规格要求；目录即项目，删目录即删项目，便于人工排查 |
| 14 | 端口 | 默认 8010（非常用端口，避开并行项目的占用） | 本机同时有另一台 AI 在跑别的服务 |

---

## 四、已知限制

1. **截图缺失（取证方式已替代）**：本次 Qoder 内置浏览器处于 `visibilityState=hidden`、viewport `0x0`，
   `take_screenshot` 与指针 `click` 均返回 `NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`。
   改用 ①可访问性树快照 ②`evaluate_script` 触发真实 DOM 事件并直接读取 canvas 像素 两种方式取证，
   证据强度等价（像素级），但没有可视化截图。人工打开 `http://127.0.0.1:8010/` 即可看到完整界面。
2. **预览动画在隐藏标签页下暂停**：星图页用 `requestAnimationFrame` 驱动绘制，页面不可见时浏览器不发帧。
   实测（`rafFired=false`）确认这是浏览器行为而非代码缺陷；手动调用页面自带的 `frame()` 后立即出星（2915 亮像素）。
3. **`CALIB` 是推导坐标，不是人工标定**：参考实现的 `CALIB` 来自人工在底图上点位。本产品用确定性算法生成星位，
   因此**换一张真实校园底图时，星星不会自动落在猫实际出没的地理位置上**——分区只保证「同区聚拢、区间分开」。
   要做到地理贴合，需要 F8「底图标定」交互（见次日待办）。
4. **照片压缩是有损的**：统一转 JPEG 并二分搜索质量以满足 200KB，PNG 透明通道会被铺平；
   极端大图（>10MB）会先逐级降采样，可能损失细节。原图不会被修改，压缩产物只落在 `workspace/`。
5. **zip 照片上传只解一层**：`photos` 接口支持 zip，但只读取 zip 内的图片条目，不递归处理套娃 zip。
6. **F6 归并建议是启发式**：按「主色 + 出没区」聚类给候选组，不做图像相似度比对；
   同色同区的两只不同猫会被放进同一候选组，仍需人工确认（草稿行的备注里已写「草稿待人工确认」）。
7. **多猫同框照片**：解析器识别 `猫数量>1` 并告警，但不会自动拆成多行编号，需人工归并后拆分。
8. **无鉴权、单机单用户**：只监听 127.0.0.1，无并发写保护（同一项目并发上传会互相覆盖），
   不符合公网部署条件——这也符合本次「不部署公网」的红线。
9. **提交历史是回溯分组**：见第一节说明。

---

## 五、次日待办

**高优先（影响可用性）**

1. **F8 底图标定**：预览页支持在真实校园底图上拖星定位，把结果写回 `CALIB` 并持久化到项目目录，
   解决「星位与地理位置不贴合」这一最大缺口。
2. **归并工作台**：把 F6 的候选组做成并排对比界面（照片 + 特征 + 场景），一键「判定同猫 / 不同猫」，
   决策写回操作日志，让 F7 摘要有真实的决策依据可写。
3. **名册在线编辑**：校验报告里的 error/warning 行支持就地修改并重跑校验，避免「下载 CSV→改→再上传」的往返。

**中优先（健壮性）**

4. zip 上传递归解包 + 单文件体积上限与总数上限保护。
5. 大项目（>200 只猫）生成耗时优化：照片压缩并行化（`ProcessPoolExecutor`），inline 分片流式写出。
6. 项目列表分页与清理命令（`scripts/cleanup.py --before 7d`），`workspace/` 会随验收跑测不断膨胀。
7. 并发保护：项目目录级文件锁，或对同一 `pid` 的写操作串行化。

**低优先（体验/工程）**

8. 星图模板主题化（配色/字体/页脚署名可配），支持学校自定义 LOGO。
9. 前端加进度条（当前只有 Toast，76 张照片压缩约需数十秒，用户看不到进度）。
10. `docs/` 补一份《跨校落地手册》：从普查到出图的完整 SOP，含常见 CSV 脏数据清单。
11. CI：把 `pytest -q` + `acceptance.py --case demo2` 放进 GitHub Actions（demo2 不依赖 E 盘，可在 CI 跑）。

---

## 六、红线自查

| 红线 | 状态 |
|---|---|
| 严禁修改 E 盘任何文件 | ✅ 全程只读（`read_bytes` / `read_text`），无任何写入 E 盘的调用。**客观复核**：冲刺结束后扫描 `E:\猫咪星图_总库` 全库，最新文件修改时间为 `2026-09-06 01:39:04`，早于本次开工时间 22:00 |
| 严禁改动 `D:\code`、`D:\校园基米skill` | ✅ 未触碰，所有写入限定在 `D:\QoderProjects\catgalaxy-factory\` |
| 不部署公网、不注册外部服务 | ✅ 服务只监听 `127.0.0.1:8010`，无外发请求 |
| 不下载 >100MB 依赖 | ✅ 仅 fastapi/uvicorn/python-multipart/pillow/pytest/httpx |
| 所有产出仅写入项目根目录 | ✅ 代码、测试、文档、验收产物、`workspace/` 均在项目内 |
| 需决策处自选默认并记录 | ✅ 见第三节 14 条 |
