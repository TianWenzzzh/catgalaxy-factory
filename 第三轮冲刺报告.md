# 第三轮冲刺报告 · 喵星图工厂 CatGalaxy Factory

- 时间：2026-09-07 04:00–06:05（无人值守自主延续段；用户指令「继续自动完成能完成的所有任务一直到早上七点五十」）
- 范围：《第二轮冲刺报告》§七「如果还有第三轮」7 条清单 + §五 限制复核
- 结果：**7 条里落地 4 条**（§七-1 可视化回归、§七-2 感知哈希预排序、§七-4 名册增删行、§七-6 workspace 清理实跑），
  用例 648 → **721**（20 → 22 个测试文件），acceptance 119 → **141 ✅ / 0 ❌**，新增 6 张浏览器实证截图且截图能力固化为可复现脚本。
- 提交：`2eb7cdf` → `dc3b0ad` → `64a3fe2` → `e1bcc7b` → 本报告（共 35 个提交）
- **补记**：本报告全部数字是 **06:05 那个时间窗的实测快照**，未回填。报告提交（06:03）之后的延续段又落地第二轮 §七-3 的一半
  —— F8 批量标定的**整分区平移**：实作与取证 06:05–06:45（七张图 06:40 一次跑齐），会话中断后 22:00 恢复、22:13 提交 `4de7006`。
  落地数 **4 → 5 / 7**，用例 **721 → 726**（22 → 23 文件），实证图 **六张 → 七张**。细节、证据与「刻意没做」的部分见 §八；
  正文 §一/§二/§四 的 06:05 数字保持原样。

---

## 一、交付总览

| # | 第二轮待办 | 第三轮做了什么 | 提交 | 用例 |
|---|---|---|---|---|
| 1 | §七-1 真浏览器可视化回归 | 截图能力固化为 `scripts/shot.py`（自起 headless Edge + CDP），六张实证图落 `docs/截图/`，方法学写进验收证据 §8 | `e1bcc7b` | +1 |
| 2 | §七-2 F9 感知哈希预排序 | `app/phash.py` 高通 dHash + `merge.annotate_visual`：组内按「长得像不像」排先后、标出每只最像谁；视觉分不掺进可疑度 | `64a3fe2` | +29 |
| 3 | §七-4 名册增删行 | `POST/DELETE /roster/rows`（物理行号坐标与 PATCH 同源）+ 前端两个删除入口；增行不替用户编编号 | `2eb7cdf` | +35 |
| 4 | §七-6 清 workspace | `cleanup.py --exclude` 保护名单（名单不匹配即退出码 2 拒跑）+ 实跑回收 1.1GB | `dc3b0ad` | +8 |

用例增量按提交实测（`git show <h> -- tests` 数新增 `def test_`，与 collect-only 对齐）：
35（名册行 28 + 控制台接线 7）+ 8 + 29（视觉 27 + 接线 2）+ 1（声明守卫）= 73 = 721 − 648。

**没做的三条与原因**：§七-3 F8 批量标定（整块新功能，见 §七）、§七-5 推远端（撞「不注册外部服务」红线，保持 blocked）、
§七-7 并行工数自适应（收益小、风险面大，留给有真实大机器数据的第四轮）。

---

## 二、逐项实测

### 1. F10 增行/删行（`2eb7cdf`）

端点坐标沿用 PATCH 那套物理行号（表头是第 1 行）：`POST /api/projects/{pid}/roster/rows`（body 带 `after` 行号）
与 `DELETE .../roster/rows?line=N`。acceptance 场景 2 的实测行：

```
F10 增行不替用户编编号（编号留空就该报错） ✅ 新行落在第 78 行，数据行 76 → 77；该行问题 ['E_BAD_ID', 'I_THIN_FEATURE']；编号列实值「」
F10 由人定号后校验转绿                  ✅ 编号 CAT-081（避开已用的 76 个号），可入图 77 行，错误 0 警告 8 提示 29
F10 新增的猫进了星图产物                ✅ CATS 77 条（原 76 条 + 新增 1 条），zip 14394KB
F10 删行：名册回到原样，照片留在工作区并报「未使用」 ✅ 删掉第 78 行（编号 CAT-081），数据行 77 → 76；工作区照片 76 张未减少；I_PHOTO_UNUSED 1 条
F10 删掉的猫不再出现在星图里            ✅ CATS 回到 76 条（期望 76）
```

两条**刻意的不自动化**：增行不替用户编编号（「弃用编号不复用」是数据红线，编号是人的决定，机器编了就等于悄悄复用体系）；
删行不删照片（照片是资产、名册是台账，删台账行不该顺手销资产——只报「未使用」提示）。
删除给两个入口：问题行右侧「✕ 删这行」+ 工具条按编号挑的「－ 删一行」——因为前者只覆盖问题行，
干净的行想删就得先故意改坏，那是在逼用户造假数据。

### 2. cleanup.py --exclude（`dc3b0ad`）

第二轮量过 1.4GB 却没敢 `--apply`：待删名单里有被《交付报告》红线补注引用的证据项目。第三轮给脚本加保护名单并实跑：

```
实跑（2026-09-07 04:45，--before 1h + 5 项排除）：删 43 个项目、回收 1.1GB、failures 为空
66 项目 / 1.7GB → 23 项目 / 645.9MB；5 个保护项逐个复核仍在
```

保护名单不是猜的：grep「文档里真被引用过的项目 id」得来（`1788707460-示例校`、`1788707789-示例校`、
`1788727122-示例校`、`1788727124-中北大学`、`_ui_demo`）。名单里有项没匹配到任何项目时 `--apply` 直接退出码 2
拒绝执行——写错了名字还照删，等于把「我以为保住了」变成一句空话。

### 3. F9 视觉预排序（`64a3fe2`）

`app/phash.py`：灰度 → 64×64 → 减一层高斯模糊（半径 2.0）做高通 → 9×8 LANCZOS → 64 位 dHash。
两条实测换来的取舍（已写进模块 docstring）：

- **朴素 dHash 会被共用背景带跑**：两张**不同**场景的合成图相似度 0.844；高通后降到 0.625，
  而同一张图缩放重压的副本仍 0.953~1.000——该认的认得出，不该认的分开。
- **无结构图不发假指纹**：纯色/整片过曝/纯噪点的哈希全是 `0`，会让白图和黑图互报「100% 相似」；
  故高通采样离中性灰不足 2 级（`MIN_CONTRAST = 2`）返回 `None`（「没有指纹」），成员沉到组尾并写明原因。

acceptance 两个场景各加 5 条证据（节选场景 2）：

```
F9 组内按视觉相似度排序，最像的两只排在最前 ✅ 顺序 CAT-001 → CAT-002 → CAT-003 → CAT-004；视觉 100% / 100% / 61% / 0%
F9 每张卡片标出组里最像它的那一位           ✅ 甲↔乙 互为最像，视觉 100.0%（同一张图重压过仍认得出）；组内最高 100.0%
F9 视觉分不掺进可疑度（判定仍然是人的事）    ✅ 可疑度仍是 0.9（0.35 + 0.55 × 特征描述相似度 100%），与视觉分 100% 各算各的
F9 纯色照片拿不到指纹，沉到最后而不是发个假 100% ✅ 丁纯色无结构 → 不算指纹，4 只里 3 只参与排序；stats.hashed_groups 1
```

契约面：`annotate_visual` 只**添加** `visual` / `visual_peer` / `visual_top` / `visual_hashed` 四个字段，
`score` / `gid` / `reason` 一个字节没动——已存判定与 F7 摘要的契约不变，有用例锁着。哈希经回调注入，
`merge.py` 不碰 Pillow 与文件系统。

### 4. 截图能力固化（`e1bcc7b`）

第二轮的「viewport 0×0 拍不了图」先被环境解除：内置浏览器实测 **724×848 @ dpr1.5**（第一/二轮是 0×0）。
但「此刻可用」不可复现（窗口最小化就回到 0×0），所以把能力固化成 `scripts/shot.py`：
自起 headless Edge（CDP 9222、独立 profile）→ `Emulation.setDeviceMetricsOverride` 统一 1080×1350@1.5
→ 页内派发**真实 DOM 事件**推状态 → `Page.captureScreenshot`。2026-09-07 05:39 一次跑齐六张并逐张目视复核：

| 文件 | 图里看得见的证据 |
|---|---|
| `F8-人工标定2颗-烘焙进页脚.png` | 页脚「星位人工标定 2/4」；F8 卡「已人工标定 2 颗 ｜ 来源：人工（2026-09-07 05:39:07）」；星点偏离算法位 |
| `F9-归并工作台-视觉预排序.png` | 中北大学真数据共用照片组：两卡「视觉相似度 100% · 最像 CAT-05x」互指；徽章「视觉最高 100%」「视觉排序 2/2 组」 |
| `F9-无结构照片不发假指纹.png` | 甲↔乙 100%、丙 61%、**丁「视觉相似度 —（这张图解不开，排不出先后）」** |
| `F4-星图预览-真实渲染.png` | 76 星全量：分区标签、毛色图例（狸花 22 … 纯黑 1）、页脚「星位为算法推导（未人工标定）」 |
| `F10-名册在线编辑-问题行摊开改.png` | 第 8/12/15 行 W_BAD_CONFIDENCE 摊开，输入框带验收笔迹「中高（验收笔迹）」，按钮「应用 2 处改动并重跑校验」 |
| `F11-主题-晨曦橘预设.png` | 预设卡「晨曦橘」高亮、九格色板换肤、toast「已切到『晨曦橘』，自定义色已清空·记得重新生成星图」 |

方法学细节（等待信号表、唯一桩 `setPointerCapture` 的披露、复现三命令）在 `docs/验收证据-浏览器实测.md` §8。
两条踩坑教训值得留档：① `#btnPreview` 在生成期间**不**禁用，早点了会载入旧 html——第一轮就因此拍出
「页脚还是未人工标定」的假证据，现在等 `#btnGenerate` 复原；② 服务端 `PUT /calib` 是**合并**不是覆盖，
截图前不清空就会把「2 颗」顶成「4 颗」，图与文件名对不上。

---

## 三、决策记录（第三轮）

| # | 决策 | 理由 |
|---|---|---|
| 1 | 视觉分只决定组内先后，**不掺进可疑度** | 像不像跟是不是重复建档是两件事；判定始终由人下，机器只排序 |
| 2 | 无结构图返回「没有指纹」而不是 0 | 哈希全 0 会让白图黑图互报 100%；假证据比没证据坏 |
| 3 | 高通 dHash 而非朴素 dHash | 实测共用背景把不同场景带到 0.844；高通后 0.625，副本仍 ≥0.953 |
| 4 | 增行不替用户编编号、删行不删照片 | 编号体系与资产处置都是人的决定；机器代办会悄悄破坏「弃用编号不复用」 |
| 5 | 删行给两个入口（问题行右侧 + 工具条按编号） | 只有问题行能删 = 逼用户先改坏数据才能删干净行 |
| 6 | `--exclude` 名单有项没匹配到即退出码 2 | 保护名单写错还照删，保护就是空话；宁可拒跑 |
| 7 | 保护名单 grep 自文档引用，不靠记忆 | 名单的权威来源是「哪些项目 id 真被交付文档引用」，不是印象 |
| 8 | 截图用自写 CDP 驱动，不上 Playwright | Playwright 要另下浏览器二进制，撞「不下载 >100MB 依赖」红线 |
| 9 | `websockets` 显式声明进 dev extra + 用例守卫 | 直接 import 只靠 `uvicorn[standard]` 传递依赖，extra 一变截图脚本当场哑火 |
| 10 | 截图等待信号只认应用自己的状态 | 静态说明文案里的「错误/警告」会立刻误判通过；toast / 按钮 disabled / `#calibState` / 模板 `#tip` 才不会骗人 |

---

## 四、证据汇总

- **pytest**：`--collect-only -q` 数出 22 文件 / 721 条；实跑进度点 10 行×72 + 1 = 721，无 `F` / `E` / `s`，退出码 0。
- **acceptance**（`scripts/acceptance.py --case all`）：场景 1 **71 ✅** + 场景 2 **70 ✅** = **141 ✅ / 0 ❌**
  （第二轮 119；第三轮 F10 增删行 +12、F9 视觉 +10）。
- **截图**：6 张 png（05:39 一次跑齐）+ 逐张目视复核记录（§二-4 表）；复现命令见验收证据 §8.2。
- **清理实跑**：43 删 / 1.1GB 回收 / failures 空 / 5 保护项复核仍在（§二-2）。
- **进程收尾**：8013（uvicorn）与 9222（headless Edge）于 06:00 前确认停止、端口释放（按命令行精确匹配杀进程，未碰用户日常浏览器）。

---

## 五、红线自查（第三轮）

| 红线 | 状态 |
|---|---|
| 严禁修改 E 盘任何文件 | ✅ 本轮代码/文档/截图全程只写项目内；E 盘归档（§六）为**纯新增**子目录 `14_喵星图工厂产出\第三轮\`，全部新路径，既有 00~13 目录与第二轮归档一个字节未动 |
| 严禁改动 `D:\code`、`D:\校园基米skill` | ✅ 未触碰 |
| 不部署公网、不注册外部服务 | ✅ 服务只监听 127.0.0.1；收尾时 8013 / 9222 已停、端口释放；headless Edge 用独立 profile，不碰用户浏览器配置 |
| 不下载 >100MB 依赖 | ✅ **本轮零新增第三方依赖**；`websockets` 本就随 `uvicorn[standard]` 在场，只是改为显式声明；刻意不引 Playwright 即为此 |
| 所有产出仅写入项目根目录 | ✅ 代码、测试、文档、截图、`workspace/` 均在项目内 |
| 需决策处自选默认并记录 | ✅ 见第三节 10 条 |

---

## 六、归档与交接

- E 盘归档（纯新增）：`E:\猫咪星图_总库\14_喵星图工厂产出\第三轮\`
  ——本报告、第三轮版 `验收证据-浏览器实测.md`、六张截图。不覆盖任何既有文件。
- workspace 现状：23 个项目 / 约 646MB（含 5 个保护项）；截图跑测只更新既有项目，未新增。
- 仓库：35 个提交，工作区干净（除本报告提交外无未跟踪文件）。

---

## 七、如果还有第四轮

按「对交付质量的影响」排序：

1. **F8 批量标定**（第二轮 §七-3 原样保留）：按分区整体平移/缩放 + 吸附底图轮廓。76 只逐星拖点仍要十几分钟，
   这是目前人工成本最大的一块。
   **（报告后续延续段已做「平移」：`4de7006`，标定模式下 `Shift+拖拽` = 锚点星所在分区全体按同一位移平移，5 条用例 + 第七张实证图，详见 §八。第四轮这条相应收窄为**分区缩放 + 吸附底图轮廓**两样；「76 只逐星拖点仍要十几分钟」只在**逐只精修**时成立，整片偏移的区现在一次手势就够。）**
2. **截图进 CI**：现在六张图是「本地一条命令复现」级别，不是「每次提交自动跑」级别；
   runner 上装 headless Edge 或退而求其次做无头 smoke 断言（canvas 像素统计进 pytest）。
3. **F9 召回层**：组内排序已用感知哈希，但召回仍是启发式（共用照片 / 同毛色+同区域）；
   把 phash 当第三路召回会改变**组成员**，动已存判定的契约，需要单独的红线评估再动。
4. **并行工数自适应**（第二轮 §七-7）：写死 4，大核机器有余量没吃；等有一份真实大机器耗时数据再做。
5. **多猫同框**（第二轮 §五-7）：解析器只告警，仍需人工拆行。
6. **推远端让 CI 真跑**（第二轮 §七-5）：保持 blocked——撞「不注册外部服务」红线，除非用户当面解除。

---

## 八、补记：F8 批量标定·整分区平移（报告后续延续段，`4de7006`）

本节是 §一 表格里缺的第 5 条。报告 06:03 提交后仍有时间，就把第二轮 §七-3「F8 批量标定」做了**平移**这一半；
正文 §一/§二/§四 的 06:05 快照不回填，所有增量只记在这里。

### 8.1 做了什么

标定模式下按住 `Shift` 拖任意一颗星 → 它所在**出没分区**的星全体按同一位移平移。改动全在 `app/templates/starmap.html` 的交互段：

- `pointerdown`：命中星且 `shiftKey` 为真时建 `dragZone`，记下分区、锚点星、起始归一化点，以及**该分区每颗星在按下瞬间的位置快照**（`base`）；否则走原来的单星 `dragCat`。
- `pointermove`：算一份 `ox/oy`，对分区每个成员取 `base + o`、`clamp(…,0,1)`、写进 `USER`，并把 hover 提示更新为锚点星。
- `pointerup`：照旧 `saveUserPos()` 落 localStorage，然后清 `dragZone`。

后端**零改动**：`calib.json` 仍是「每颗星一份 0~1 归一化坐标」，`PUT /calib` 的合并语义、页脚「人工标定 n/m」的口径都没变——批量只是**写入方式**，不是新的数据形态。

### 8.2 决策记录（本节 5 条，接 §三 的 10 条）

| # | 决策 | 理由 |
|---|---|---|
| 11 | 位移基准取**按下瞬间的快照**，不是「上一帧位置累加」 | 累加写法里，被 `clamp` 卡在边界的星会永久失去同步（往回拖时它跟不上），一次手势就把分区拖散；快照写法保证「一次手势 = 一次平移」，来回拖不放大漂移 |
| 12 | 纯客户端实现，不动接口与存储契约 | 分区平移的产物与逐星拖完全同构（还是那批归一化坐标），没有理由为它加一个后端端点；已存标定、F7 摘要、页脚口径因此全不受影响 |
| 13 | 没拖动的纯 `Shift` 点击仍然开档案卡 | 新分支不能吞掉既有交互：开卡的守卫条件是 `dragging && !moved && !dragCat && !dragZone`，`!dragZone` 是新加的（有用例锁） |
| 14 | 只做平移，**不做缩放与建筑物吸附** | 缩放要定义锚点与比例、并处理夹取后的形变（坐标契约扩张）；吸附要先在底图上认出轮廓（新的图像处理面）。两者都不是「客户端加个分支」的量级，不该塞在通宵段尾巴上 |
| 15 | 能力写进 hover 提示与 F8 卡片文案 | 藏在 `Shift` 里的功能等于不存在；模板提示改为「（Shift+拖拽=整分区平移）」，F8 卡加一句「整片偏了不用逐星拖」（有用例锁两处文案） |

### 8.3 证据（四层）

1. **pytest**：`--collect-only -q` 逐文件求和 **23 文件 / 726 条**（新增 `tests/test_batch_calib.py` 5 条）；
   实跑进度点 10 行×72 + 6 = **726**，无 `F` / `E` / `s`，退出码 **0**。
2. **浏览器内行为断言**（比截图更早把关）：`scripts/shot.py` 新增 `shot_zone_shift`，在真 headless Edge 里派发
   `shiftKey` 的 `pointerdown → pointermove → pointerup`，然后读模板自己暴露的 `__catgalaxy.getUserCalib()`，
   **要求键数恰为 4**（视觉验收校同区 4 只），少一颗直接 `RuntimeError` 中断——「Shift 事件发出去了」不等于「四颗星都写了」。
3. **第七张实证图** `docs/截图/F8-分区平移-Shift拖拽.png`（06:40 与其余六张一次跑齐）：
   页脚「星位人工标定 **4/4**」；F8 卡「名册 4 颗星 ｜ 已人工标定 4 颗 ｜ 算法推导 0 颗 ｜ 来源：人工（2026-09-07 06:40:29）」；
   卡片说明里那句「整片偏了不用逐星拖：**Shift+拖拽**一颗星 = 它所在出没分区全体一起平移」也在图上。
4. **acceptance 重跑**：模板改动会影响所有新生成的产物，故重跑 `--case all` 复核 —— **141 PASS / 0 FAIL**，
   且产物 HTML 字符数如 §8.5 所列真的变了（28915 → 29840），证明这是「改动了产物之后仍然全绿」，不是空跑。

诚实披露两条：`shot.py` 里 `Shift` 是合成 `PointerEvent` 的 `shiftKey: true` 标志位（没有真按物理键），
`setPointerCapture` 的桩与第三轮一致（`docs/验收证据-浏览器实测.md` §8.4）；每次标定截图前都先清空标定，
因为服务端 `PUT /calib` 是**合并**不是覆盖，不清空就会把「4/4」变成残留堆出来的假数字。

### 8.4 文档同步范围

README（F8 卡片行 + 23/726 + 目录树「七张」）、`docs/跨校落地手册.md` §7（新增第 3 步：整片偏移用 Shift+拖）、
`docs/验收证据-浏览器实测.md` §8（复现命令与 §8.5 改七张、§8.3 新增「分区平移生效」等待信号行、§8.4 披露 `shiftKey`）、
《第二轮冲刺报告》§五-2 与 §七-3 就地标注 + 头部指针。本报告正文数字**不回填**，增量只在本节。

### 8.5 复验与红线（本节）

- **pytest**：23 文件 / 726 条全绿（§8.3-1）。
- **acceptance**（22:16 重跑 `--case all`）：**141 PASS / 0 FAIL**（场景 1 **71 ✅** + 场景 2 **70 ✅**），退出码 **0**——与 06:05 的 141 条**逐条同数**，即模板改动没有碰坏任何一条契约断言。
- **产物确实变了**（不是「跑了个没影响的回归」）：重新生成的两份验收证据 diff 只有项目 id（时间戳）与产物字符数——
  relative `28915 → 29840`、inline `28962 → 29887`、zip `379KB → 380KB`。多出来的约 925 字符就是分区平移那段交互与加长的 hover 提示；
  换句话说，**新能力进了交付产物，而 141 条证据一条没退**。
- 红线：只写项目内；无新增第三方依赖（纯前端分支）；E 盘归档仅**新增**第七张图与本补记，不覆盖任何既有文件；服务仍只听 127.0.0.1。
- 未做且明确留给第四轮：分区缩放、吸附底图轮廓（决策 14）、§七 第 2/3/4/5/6 条原样保留。
